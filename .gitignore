node_modules/
.expo/
dist/
build/
Payload/
# Prebuild output at the repo root only. These patterns MUST stay anchored with
# a leading slash: a bare `ios/` also matches modules/alarm-kit/ios, which
# silently excluded the AlarmKit Swift source from every build.
/ios/
/android/
*.log
.DS_Store
npm-debug.*
*.orig.*
.env
.env.local
app.json
